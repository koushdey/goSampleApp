apiVersion: v1
kind: Service
metadata:
    name: "aks1kodey"
    labels:
        app: "aks1kodey"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "aks1kodey"
