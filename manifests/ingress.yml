apiVersion: extensions/v1beta1
kind: Ingress
metadata:
    name: "aks1kodey"
    labels:
        app: "aks1kodey"
    annotations:
        kubernetes.io/ingress.class: addon-http-application-routing
spec:
    rules:
        - host: aks1kodeytest-aks1kodey.754755bb443d44b19cee.centralus.aksapp.io
          http:
            paths:
            - path: /
              backend:
                serviceName: "aks1kodey"
                servicePort: 80
